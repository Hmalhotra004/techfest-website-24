import styles from "./expo.module.scss";

const page = () => {
  return (
    <section className={styles.bg}>
      <section className={`container mainP`}>
        <h1>TechExpo</h1>
      </section>
    </section>
  );
};

export default page;
